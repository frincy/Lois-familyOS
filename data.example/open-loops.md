# Open loops

Single source of truth for everything unresolved. The order is yours — top of Active = highest priority. The Chief of Staff keeps this file tidy; it never silently re-ranks.

Format of each Active item:
`- [ ] {title} · {context} · due:YYYY-MM-DD (if any) · next: {action} #category opened:YYYY-MM-DD`

Hidden tokens (`opened:`, `#category`, `src:`, `ref:`) power the radar / board / knowledge links. Don't read them aloud.

## Active  (top = highest priority — reorder freely)

(none yet — chat with the COS to add some)

## Done (recent)

(none yet)
